node_modules/
.next/
out/
.env*.local
.DS_Store

# auto-deploy script log (written into repo root by scripts/auto-deploy.sh)
auto-deploy.log

# internal QA / audit reports — flagged to Max for disposition
TESTREPORTS/
