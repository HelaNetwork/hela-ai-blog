# HeLa AI Blog — Design Document

## Architecture

Static Next.js 14 site (App Router) with MDX content, exported to static HTML via `output: 'export'`. Deployed to Cloudflare Pages via Wrangler. No server-side runtime — all rendering is at build time.

## Tech Stack

| Layer | Technology |
|-------|-----------|
| Framework | Next.js 14 (App Router) |
| Content | MDX via `next-mdx-remote`, frontmatter via `gray-matter` |
| Styling | Tailwind CSS 3 + `@tailwindcss/typography` + custom CSS variables |
| Fonts | Press Start 2P (headings), VT323 (body) — Google Fonts |
| Design | Retro pixel-art theme (see `public/preview.html` for reference) |
| Images | PIL-generated 800x400 post headers in `public/images/posts/` |
| Dates | `date-fns` |
| Deployment | Cloudflare Pages (via `wrangler`) |
| Comments | Giscus (GitHub Discussions, external script) |
| Export | `next build` → static `out/` directory |

## Design System (2026-03-30 — pixel-art restyle)

**Colors:** `--bg #080810`, `--panel #0f0f1c`, `--card #13131f`, `--border #252540`, `--accent #C6F135` (lime), `--accent2 #ff3c6f` (pink), `--accent3 #ffe94d` (yellow), `--accent4 #3cf0ff` (cyan), `--text #c8c8e8`, `--dim #5a5a7a`

**Components (CSS classes in globals.css):** `.pixel-nav`, `.ticker`, `.pixel-card`, `.post-tag`, `.char-slot`, `.ground-line`, `.pixel-footer`, `.pixel-bar`

**Header images:** Generated by Python PIL script (documented in AUDIT.md). Each 800x400 image: dark grid background + character PNGs from `~/projects/media-factory/output/pixel-chars-poses/{name}/{pose}.png` scaled to 192px, accent bar at top. Saved to `public/images/posts/{slug}.png`.

## File Map

| Path | Purpose |
|------|---------|
| `app/layout.jsx` | Root layout — fonts, metadata, nav shell (HOME / DOCS / HIP / HELA LABS / HELASYN / RSS) |
| `app/page.jsx` | Homepage — featured post hero + post grid |
| `app/posts/[slug]/page.jsx` | Individual post renderer (MDX via next-mdx-remote, GFM enabled) |
| `app/docs/page.jsx` | HelaSyn docs — sidebar + JSX-rendered sections |
| `app/hip/page.jsx` | HeLa Improvement Proposals — sidebar + per-HIP sections (HIP-001 LIVE, 002/003 PROPOSED) |
| `app/hip/layout.jsx` | HIP layout with `BreadcrumbList` + `TechArticle` JSON-LD for HIP-001 |
| `app/drafts/page.js` | Internal drafts listing (no auth — publicly accessible) |
| `app/rss/` | RSS feed generation |
| `app/sitemap.js` | Sitemap generation |
| `app/globals.css` | Global CSS — includes `.prose table/th/td` GFM table styling |
| `lib/posts.js` | `getAllPosts()`, `getPostBySlug()`, `getAllSlugs()` — filesystem MDX parser |
| `components/PostCard.jsx` | Post card UI (featured and grid variants) |
| `components/GiscusComments.jsx` | Giscus comment widget wrapper |
| `content/posts/` | Published MDX blog posts |
| `content/plans/` | Internal team plans (content only, no route?) |
| `content/drafts/` | Draft posts |
| `content/press/` | Press content |
| `content/social/` | Social content |
| `next.config.js` | `output: 'export'`, `trailingSlash: true`, images unoptimized |
| `out/` | Built static output — deployed artifact |
| `SECURITY_REVIEW.md` | Seth's security review (2026-03-24) |

## Content Structure

Posts are MDX files in `content/posts/`. Frontmatter fields: `title`, `date`, `author`, `tags`, `summary`, `image`. Posts are sorted by date descending. No database — all content is filesystem.

`lib/posts.js` reads files at build time with `fs.readdirSync` and `gray-matter`. No try/catch around file reads (build-time risk only; no runtime impact).

## MDX Rendering Chain

`app/posts/[slug]/page.jsx` uses `next-mdx-remote/rsc` with the `remark-gfm` plugin to support GitHub-Flavored Markdown — including pipe-delimited tables, strikethrough, autolinks, and task lists. Tables are styled via `.prose table/th/td` selectors in `globals.css`, matching the pixel-art theme used by the JSX `Table` helper in `app/docs/page.jsx`.

Custom MDX components: `YouTubeEmbed`, `ImageFull`, `VideoEmbed` (from `components/MediaEmbed`). Components are resolved via the `components` prop on `MDXRemote` — `next-mdx-remote/rsc` does NOT honor relative `import` statements written inside `.mdx` files, so posts must use registered component names without an import line.

## Routes

| Route | Type | Notes |
|-------|------|-------|
| `/` | Homepage | Featured post + grid |
| `/posts/[slug]` | Static | One per MDX file in `content/posts/` |
| `/docs` | Single page | HelaSyn framework docs, sidebar nav |
| `/hip` | Single page | HeLa Improvement Proposals — all HIPs rendered in static HTML, hash routing for active section, deep-linkable as `/hip#hip-001` etc. |
| `/drafts` | Static | Internal drafts list (no auth) |
| `/rss` | Static | RSS feed |
| `/sitemap.xml` | Static | Sitemap |

## Data Flow

1. `next build` → `lib/posts.js` reads `content/posts/*.mdx`
2. `getAllPosts()` returns sorted post metadata array
3. `getPostBySlug(slug)` returns full post content for dynamic `[slug]` route
4. Static HTML generated for all slugs at build time
5. `out/` directory uploaded to Cloudflare Pages via `wrangler pages deploy out/`

## Deployment

- **Build:** `npm run build` (Next.js static export to `out/`). A `prebuild` hook runs `scripts/check-images.js` first and fails the build if any post's frontmatter `image:` does not resolve under `public/`. `<VideoEmbed>` references print a non-fatal warning. Tests at `scripts/test-check-images.js` (positive + negative + mixed + video-warn), invoke via `npm run test:check-images`.
- **Deploy:** `wrangler pages deploy out/ --project-name hela-ai-blog`
- **Auto-deploy:** `scripts/auto-deploy.sh` watches `content/posts public/images/posts public/videos`; gates on `main` branch only (refuses to deploy from any other checked-out branch). The production branch is `main` (wrangler deploys `--branch=main`).
- **Live URLs:**
  - `https://blog.helachain.com` (custom domain)
  - `https://eeb3fc8f.hela-ai-blog.pages.dev` (Cloudflare Pages direct)
- **First deploy:** 2026-03-26 by Tex, 15 pages

## SEO Setup

- `export const metadata` in each page file (Next.js App Router pattern)
- `app/sitemap.js` — generates `/sitemap.xml` at build time
- `app/rss/` — generates RSS feed
- `trailingSlash: true` in next.config.js for Cloudflare Pages URL compatibility

## Editorial Standards

External claims in blog posts and HIP pages must include a public citation. Internal-only sources (e.g., implementation files) cite the file path.

## Known Risks

- `/drafts` route is publicly accessible — no auth gate
- Giscus comment script loaded from `giscus.app` with no SRI hash
- No CSP headers configured
- `furykc` GitHub username exposed in Giscus config (links to KC's private account)
- `lib/posts.js` has no error handling on file reads

## Dependencies

```json
next, react, react-dom, next-mdx-remote, gray-matter,
tailwindcss, @tailwindcss/typography, autoprefixer, postcss, date-fns
```
